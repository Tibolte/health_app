# Health App — iOS

SwiftUI app that reads health data from HealthKit and syncs it to the web API.

## Status

Minimal hello world project. HealthKit integration is next.

## Requirements

- Xcode 15+
- iOS 17+ target

## Getting Started

Open `HealthApp.xcodeproj` in Xcode and run on a simulator or device.
